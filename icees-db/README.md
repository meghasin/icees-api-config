# ICEES DB

Tools for building the ICEES backend database(s).

## Instructions

set the following environment variables:

```text
ICEES_PORT=5432
ICEES_HOST=db
DB_PATH=./db/data
```

Run:

```bash
./initdb.sh
```

